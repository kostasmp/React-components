import React from 'react';
import ReactDOM from 'react-dom/client';
import Validationrules from './validform';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
    <Validationrules />
  </React.StrictMode>
);

